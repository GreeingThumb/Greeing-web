import { style } from '@vanilla-extract/css'

export const emailAuthWrapper = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '12px',
})

export const emailAuthContentWrapper = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '12px',
})
